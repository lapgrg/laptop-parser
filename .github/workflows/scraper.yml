name: 🔍 Парсинг ноутбуков

on:
  schedule:
    # Утром в 9:00 по Тбилиси (UTC+4)
    - cron: '0 5 * * *'
    # Вечером в 18:00 по Тбилиси (UTC+4)
    - cron: '0 14 * * *'
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout репозитория
      uses: actions/checkout@v4
      
    - name: 🐍 Установка Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: 📦 Установка зависимостей
      run: |
        pip install -r requirements.txt
        playwright install chromium
        playwright install-deps
        
    - name: 🔍 Запуск парсера
      run: python laptop_scraper.py
      
    - name: 💾 Сохранение результатов
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "🔄 Обновление данных"
        file_pattern: 'output/*'
```

4. Внизу страницы нажми зелёную кнопку **Commit changes**
5. В появившемся окне снова нажми **Commit changes**

---

#### 3.2 Создаём файл `requirements.txt`

1. Вернись на главную страницу репозитория (нажми на название репозитория вверху)
2. Нажми **Add file** → **Create new file**
3. Имя файла: `requirements.txt`
4. Содержимое:
```
playwright==1.40.0
beautifulsoup4==4.12.2
lxml==4.9.3
